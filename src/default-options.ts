import { Options } from "./types/options";

export const defaulOptions: Options = {
    frequenciesSource: 'vk',
    handlingCase: 'ignore',
    handlingYo: 'ignore',
    useLevenshtein: true,
    minAcceptibleDistance: 2,
};